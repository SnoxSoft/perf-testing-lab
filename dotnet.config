# The .NET 10 SDK no longer runs Microsoft.Testing.Platform projects through
# VSTest, and xunit.v3 is an MTP runner. Without this opt-in, `dotnet test`
# fails before a single test executes.
[dotnet.test:runner]
name = "Microsoft.Testing.Platform"
